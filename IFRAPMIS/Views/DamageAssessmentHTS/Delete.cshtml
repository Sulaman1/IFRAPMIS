@using Microsoft.AspNetCore.Authorization
@using PermissionPro.PreDefined
@inject IAuthorizationService AuthorizationService
@* @using Constant.Constants *@
@model DAL.Models.Domain.Damage.DamageAssessmentHTS

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (true)//(AuthorizationService.AuthorizeAsync(User, Permissions.Administrator.Delete)).Result.Succeeded
{
    <h2>Delete</h2>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Damage Assessment HTS</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DamageAssessmentHTSType)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DamageAssessmentHTSType)
            </dd>
            @* dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Code)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Code)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Description)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Provience)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Provience.Code)
            </dd> *@
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="DamageAssessmentHTSId" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-action="Index">Back to List</a>
        </form>
    </div>
}